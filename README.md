# JSON-Prettifier-
A Chrome extension that automatically detects JSON responses and transforms them into a clean, interactive, syntax-highlighted viewer. No more squinting at raw text, just beautiful, explorable data.
